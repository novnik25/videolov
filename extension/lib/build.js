// Отметка сборки. Меняется при КАЖДОЙ правке кода служебного скрипта.
//
// Зачем: страницы расширения (окно, настройки, разбор) браузер читает с диска
// заново при каждом открытии, а служебный скрипт живёт в памяти до перезапуска
// расширения. В Яндекс.Браузере кнопки «Обновить» на странице расширений нет
// вовсе — и человек остаётся с новым окном поверх старой начинки, не понимая,
// почему правка не подействовала. Сравнение этих отметок ловит расхождение,
// а перезапуск делается изнутри: chrome.runtime.reload().
export const BUILD = "2026-09-17-03";
